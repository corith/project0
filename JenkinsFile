pipeline {
    agent any {
        stages {
            stage ('Build') {
                steps {
                    sh 'mvn clean package'
                }
                post {
                    sh 'cp target/project0.war ~/apache-tomcat-9.0.40/webapps'
                }
            }
        }
    }
}
